{
  "name": "@hexnome/rules",
  "version": "0.0.0",
  "private": true,
  "//": "Source only - no build, no dist. Both consumers compile it themselves: the frontend through a Vite alias and a tsconfig path, the backend through a symlink at backend/src/rules that puts these files inside its own program. So there is no build step to forget and no compiled copy to go stale.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
